export type TimeField = {
    name: string;
    value: string;
}

export type GuildCategory = {
    guild_id: string,
    category: string,
    message_id: string,
}

export type GuildBoss = {
    boss: string,
    guild_id: string,
}
