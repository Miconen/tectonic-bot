version: "3.7"

services:
    app:
        build: .
        command: node build/main.js
        env_file:
            - .env
        environment:
            API: http://api:8080/api/v1
        networks:
            - bot-network

networks:
    bot-network:
        external: true
        name: tectonic-api_default
